# This Netlify site is retired. The assistant now lives only on Vercel (Poe).
# public/_redirects 301s every path to the Vercel URL so the original Netlify
# link the committee chairman was given never breaks. Nothing is built here.
[build]
  publish = "public"
